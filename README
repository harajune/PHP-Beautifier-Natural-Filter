ある程度人間が整形したものを許容する様にするためのフィルター
php_beautifier のフィルタです。

使い方：
php_beautifier -d ../path/to/Naturalfilter/ -l "Natural() Pear()"

Pearなどその他のフィルタと一緒に使ってください。
改行や空行など人間をできるだけ自然な形で残す様にして
最低限の変更のみを加える様にしてあります。

ToDo:
詳しい使い方を書く
